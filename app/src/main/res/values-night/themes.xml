<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.MetalDetector" parent="Theme.MaterialComponents.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/toolbar_background</item>
        <item name="colorPrimaryVariant">@color/toolbar_background</item>
        <item name="colorOnPrimary">@color/toolbar_text</item>

        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">@color/toolbar_background</item>

        <item name="android:navigationBarColor" tools:targetApi="l">@color/gravity_card_outer</item>
        <item name="android:windowLightNavigationBar" tools:targetApi="l">false</item>
    </style>
</resources>